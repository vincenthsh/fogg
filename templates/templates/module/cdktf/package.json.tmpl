{
  "name": "{{ .Name }}",
  "version": "{{ .Version }}",
  "author": "{{ .Author }}",
  {{- if .Publish}}
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**"
  ],
  {{- else}}
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  {{- end}}
  "scripts": {
    "get": "cdktn get",
    "ca:login": "pnpm -w run ca:login",
    {{- if .Publish}}
    "build": "tsc",
    "watch": "tsc --watch",
    {{- end}}
    "lint": "eslint",
    "test": "vitest run --passWithNoTests  --coverage",
    "prettier": "prettier --write .",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist && rm -rf coverage"
  },
  "dependencies": {{ .CdktfDependencies | toPrettyJson | indent 2 | trim }},
  "devDependencies": {{ .CdktfDevDependencies | toPrettyJson | indent 2 | trim }},
  "peerDependencies": {{ .CdktfPeerDependencies | toPrettyJson | indent 2 | trim }},
  "//": "Auto-generated by fogg. Do not edit"
}
