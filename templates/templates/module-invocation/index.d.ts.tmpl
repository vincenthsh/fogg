{{ template "fogg_header_js" }}
/**
 * Outputs for:
 * - Environment: "{{ .Env }}"
 * - Component: "{{ .ComponentName }}"
 */
export interface {{ .ComponentNameTitle }}Outputs {
{{- range .Modules }}
  // module "{{.ModuleName}}" outputs
  {{- $outer := . -}}
  {{- range .Outputs }}
  /**
    * {{.Description}}
    */
  {{ jsPropName (printf "%s%s" $outer.ModulePrefix .Name) }}: any;
  {{- end }}
{{- end }}
}
