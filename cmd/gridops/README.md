# Grid Sync

This CLI will call the gridapi to ensure States exists based on marker files generated by fogg.

It will also create dependencies between states and resolve the terraform state outputs actually consumed.
