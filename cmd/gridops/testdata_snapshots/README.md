# Snapshots of GridOps integration tests

This directory contains snapshot files used in the integration tests for the GridOps CLI on fogg testdata/ folders. These snapshots capture the expected API calls and responses during test execution.
